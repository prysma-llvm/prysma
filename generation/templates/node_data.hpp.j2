//===-- node_data.hpp -------------------------------------------*- C++ -*-===//
//
// Part of the Prysma Project, under the GNU GPL v3.0 or later.
// See LICENSE at the project root for license information.
// SPDX-License-Identifier: GPL-3.0-or-later WITH Prysma-exception-1.0
//
//===----------------------------------------------------------------------===//

#pragma once

#include <utility>

#include "compiler/ast/nodes/interfaces/i_node.h"
#include "compiler/ast/registry/types/i_type.h"
#include "compiler/lexer/lexer.h"
#include <llvm-18/llvm/ADT/ArrayRef.h>

#ifndef NODE_DATA_H
#define NODE_DATA_H

// Do not modify the code; this section is dynamically generated by the Jinja2 template.
// Enum dynamically generated for all AST nodes

{% for classe in classes %}
struct {{ classe.nom }}NodeData {
{% if classe.champs %}
private:
{% for nom_champ, type_champ in classe.champs.items() %}
    {{ type_champ }} _{{ nom_champ }};
{% endfor %}
{% endif %}

public:
{% if not classe.champs %}
    {{ classe.nom }}NodeData() = default;
{% else %}
    {{ classe.nom }}NodeData() = default; 

    {{ "explicit " if classe.champs|length == 1 else "" }}{{ classe.nom }}NodeData({{ classe.params_builder }})
        : {{ classe.inits_builder }} {}

{% for nom_champ, type_champ in classe.champs.items() %}
    {{ type_champ }} get{{ nom_champ[0]|upper }}{{ nom_champ[1:] }}() { return _{{ nom_champ }}; }
{% endfor %}
{% endif %}
{% if classe.nom == "Operation" %}

    void addExpression(INode* l, INode* r) { _left = l; _right = r; } 
{% endif %}
};

{% endfor %}
#endif
