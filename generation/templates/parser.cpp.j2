#ifndef PARSER_{{nom | upper}}_CPP
#define PARSER_{{nom | upper}}_CPP

#include "{{_include_parser_}}"


// Do not modify the code; this section is dynamically generated by the Jinja2 template.
Parser{{nom}}::Parser{{nom}}(ContextParser& contextParser) 
    : _contextParser(contextParser)
{}

Parser{{nom}}::~Parser{{nom}}()
{}

INode* Parser{{nom}}::parser(std::vector<Token>& tokens, std::size_t& index)
{
  // TODO: Implement the parser for {{nom}}
  (void)tokens;
  (void)index;
  return nullptr;
}

#endif /* PARSER_{{nom | upper}}_CPP */





