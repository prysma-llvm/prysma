#ifndef EXPRESSION_{{nom | upper}}_H
#define EXPRESSION_{{nom | upper}}_H

#include "compiler/ast/nodes/expression/i_expression_engine.h"
#include "compiler/ast/registry/context_expression.h"

// Do not modify the code; this section is dynamically generated by the Jinja2 template.
class Expression{{nom}} : public IExpressionEngine
{
private:
    ContextExpression& _context;

public:
    Expression{{nom}}(ContextExpression& expressionContext);
    ~Expression{{nom}}() override;

    auto build(const std::vector<Token>& equation) -> INode* override;
};

#endif /* EXPRESSION_{{nom | upper}}_H */