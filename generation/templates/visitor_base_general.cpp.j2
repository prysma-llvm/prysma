#include "compiler/visitor/visitor_base_generale.h"
#include "compiler/ast/registry/context_gen_code.h"
#include "compiler/macros/prysma_maybe_unused.h"
#include "compiler/ast/ast_genere.h"

// Do not modify the code; this section is dynamically generated by the Jinja2 template.

VisitorBaseGenerale::VisitorBaseGenerale(ContextGenCode* contextGenCode)
    : _contextGenCode(contextGenCode)
{}

{% for nom, champs_traversables in methodes.items() %}
void VisitorBaseGenerale::visiter(Node{{ nom }}* node{{ nom }}{{ ' PRYSMA_MAYBE_UNUSED' if not champs_traversables else '' }})
{
{%- if champs_traversables %}
    auto& node{{nom}}Data = _contextGenCode->getNodeDataRegistry()->get(node{{nom}});

{%- for getter, type_champ in champs_traversables %}
{%- if type_champ == 'llvm::ArrayRef<INode*>' %}
    for (auto* element : node{{ nom }}Data.{{ getter }}())
    {
        element->accept(this);
    }
{%- else %}
    if (node{{ nom }}Data.{{ getter }}() != nullptr)
    {
        node{{ nom }}Data.{{ getter }}()->accept(this);
    }
{%- endif %}
{%- endfor %}
{%- endif %}
}

{% endfor %}
