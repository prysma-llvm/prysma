#ifndef AST_GENERE_H
#define AST_GENERE_H

#include "compiler/ast/nodes/interfaces/i_node.h"
#include "compiler/ast/nodes/interfaces/i_expression.h"
#include "compiler/visitor/interfaces/i_visitor.h"
#include <cstddef>
#include "compiler/ast/registry/types/i_type.h"
#include <llvm/ADT/ArrayRef.h>

// Do not modify the code; this section is dynamically generated by the Jinja2 template.
// Enum dynamically generated for all AST nodes
enum class NodeTypeGenerated {
{% for classe in classes %}
    {{ classe.nom }},
{% endfor %}
};

{% for nom in nodes_noms %}
class Node{{ nom }};
{% endfor %}

{% for classe in classes %}
class Node{{ classe.nom }} : public {{ classe.parent }} {
public:
    explicit constexpr Node{{classe.nom}}(std::size_t p_id) : nodeId(p_id) {}

public:
    PRYSMA_NODISCARD auto getGeneratedType() const -> NodeTypeGenerated override  {
        return NodeTypeGenerated::{{classe.nom}};;
    }

        PRYSMA_NODISCARD static auto classof(const INode* node) -> bool {
        return node->getGeneratedType() == NodeTypeGenerated::{{ classe.nom }};
    }

public: 
    PRYSMA_NODISCARD auto getNodeId() const -> std::size_t override  { return nodeId; }

    void accept(IVisitor* visitor) override {
        visitor->visiter(this);
    }
    
private:
    std::size_t nodeId;

};

{% endfor %}
#endif
