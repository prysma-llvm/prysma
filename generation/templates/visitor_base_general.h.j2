#ifndef VISITEUR_BASE_GENERALE_H
#define VISITEUR_BASE_GENERALE_H

#include "compiler/visitor/interfaces/i_visitor.h"

// Do not modify the code; this section is dynamically generated by the Jinja2 template.
{% for nom in nodes %}
class Node{{ nom }};
{% endfor %}

struct ContextGenCode;

class VisitorBaseGenerale : public IVisitor
{
protected:
    // TODO : getNodeDataRegistry est le seule utilisée dans le contextGenCode
    // Est-ce qu'il serais préférable de seulement injecter le registre ici ? 
    // Possible. 
    ContextGenCode* _contextGenCode;

public:
    VisitorBaseGenerale(ContextGenCode* contextGenCode);
{% for nom in nodes %}
    void visiter(Node{{ nom }}* node{{ nom }}) override;
{% endfor %}
};

#endif
